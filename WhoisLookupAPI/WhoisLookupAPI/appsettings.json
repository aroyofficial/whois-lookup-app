{
  "BetterStack": {
    "IngestionURL": "{{BETTERSTACK_INGESTION_URL}}",
    "APIToken": "{{BETTERSTACK_API_TOKEN}}"
  },
  "Redis": {
    "Endpoint": "{{UPSTASH_REDIS_ENDPOINT}}",
    "Password": "{{UPSTASH_REDIS_API_PWD}}"
  },
  "WhoisAPI": {
    "BaseURL": "{{WHOIS_API_BASE_URL}}",
    "APIKey": "{{WHOIS_API_KEY}}"
  }
}